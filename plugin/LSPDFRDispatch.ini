[General]
; Backend server URL (the machine running the Python backend)
BackendUrl=ws://localhost:8000
; API key must match the backend's DISPATCH_API_KEY setting
ApiKey=changeme
; Radius in game units to scan for nearby peds
ScanRadius=100
; Radius in game units to detect crime events
CrimeDetectionRadius=150
; Your officer callsign
OfficerCallsign=1-Adam-12
